#include "main.h"
/**
* _isupper - checks for uppercase character
* @c: int type number to check
* Return: 0 or 1
*/
void more_numbers(void)
{
  int i, j;
  for (i = 0; i <= 10; i++)
    {
      for (j = 0; j <= 14; i++)
	{
	  _putchar ('0' + 1);
	}
      _putchar ('\n');
    }
}
