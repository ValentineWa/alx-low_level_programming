#include "main.h"
/**
 * *_strcat - concatenates two strings
 * @dest, @src: pointer of type char
 * Return: void
 */
char *_strcat (char *dest, char *src)
{
  dest[] = "Hello";
  src[] = "world";
  _strcat (dest, src);
  return (dest);
}
