#include "main.h"
/**
 * *_strcat - concatenates two strings
 * @dest, @src: pointer of type char
 * Return: void
 */
char *_strcat (char *dest, char *src)
{
  int *a;
  a = strcat (dest, src);
  return (a);
}
