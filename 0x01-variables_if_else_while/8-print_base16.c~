#include <stdlib.h>
#include <stdio.h>
#include <time.h>

int main (void)

  int num = 48;

while(num <= 57)
  {
    putchar (num);
    num++;

  }
num =57;
while(num<= 102)
  {
    putchar(num);
    num++;

  }
putchar('\n');
return(0);
}
