#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main (void)

  char alph = 'a';

while (alph <= 'z')
  (
   if (alph == 'q')
     {
       alph++;
     }
   else if (alph == 'e')
     {
       alph++;
     }
   else
     {
       putchar(alph);
       alph++;
     }
   }
  
  putchar('\n');
   return (0);

   }
